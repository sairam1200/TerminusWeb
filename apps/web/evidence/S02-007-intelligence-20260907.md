# S02-007 private intelligence web evidence

Maker: `/root/intelligence_web`, Session 02, isolated branch `session/02-web`.
Baseline: `f3e5bd6`. Contract: S01-008 `1a9e795`, canonical authentication fixture
`79cc961`, RPC fixture `7eab6ce`; quota addendum `8de498b` and paginated export
addendum `3efefd6` from Session 01.

## Implementation

- Authenticated same-host `/intelligence` socket, `terminus.intelligence.v1`,
  domain-separated HMAC using the existing non-extractable IndexedDB pairing key.
- Closed request/result validation, 64 KiB frames, ten-second RPC/auth deadlines,
  five pending requests, three bounded retries, disposal and stale-socket guards.
- Separate explicit composer; recommendation selection only fills it. Enter is
  sent only on explicit submission. Recording is asynchronous, consent-gated,
  independent of terminal output/keystrokes, and never claims shell completion.
- Source-attributed retrieval cards accurately distinguish catalog from local
  model explanations. No hosted-model browser integration.
- `/history`, `/usage`, `/account`, `/account/privacy`, `/billing`, `/admin` with
  loading, empty, unavailable and unauthorized states. Consent defaults off,
  private account RPCs, export and owner-only deletion; no payment operations.
- Session refresh on focus/30-second intervals and result ownership fencing after
  session identity rotation. Passwords and queries remain transient browser memory.
- Existing terminal implementation changes consist only of panel composition.
  Existing controls, xterm input/output, pairing, profiles, remembered-session
  fragments, PWA and endpoint environment configuration remain in use.

## Maker commands and outcomes

Commands run from `apps/web` on Windows:

- `npm ci --no-audit --no-fund`: first sandbox run failed with `spawn EPERM`;
  authorized elevated tool execution passed, 443 packages installed. No new dependencies.
- `npm test`: PASS, 14 files / 113 tests. Socket/component doubles, not real backend proof.
- `npm run typecheck`: PASS; production build also typechecked final source.
- `npm run lint`: PASS.
- `npm run build`: PASS, Next.js 16.3.3 static production build, ten generated pages.
- `npx prettier --check intelligence components/Intelligence*.tsx components/TerminalShell.tsx app/globals.css app/account app/history app/usage app/billing app/admin`: PASS.
- `npm run format:check`: existing baseline formatting failures in 38 untouched
  files. No changed/new product file appears in the failure list.
- `git diff --check`: PASS.

Tests consume copied canonical public synthetic auth/RPC fixtures, including HMAC
domain mismatch, rejected ownership injection, consent shape, multiline input,
wrong subprotocol, expired/replayed/oversized/unknown handshake, RPC timeout,
bounded retries/concurrency, teardown, safe error, result shape, consent-off
non-recording, disconnected composer, fill-without-execute, account/guest deletion,
password clearing, empty history, unavailable usage and disabled commercial plan.

Dependency/API verification: existing `package-lock.json`, installed React/Next
types, existing `CredentialStore`, endpoint policy, codec and Web Crypto use.
No dependency or server-only environment variable was introduced. No environment
file or secret values were read, copied or committed by this maker.

## Limits and next gate

No browser, real socket/database/model, deployment, infrastructure or public
exposure action was performed by this maker. Root owns serialized Chrome staging
verification and Session 06 exact-product independent verification. No production
connectivity or model-generation success is claimed here. Independent review and
exact product SHA are recorded in the separate Session 02 status handoff.

## Independent review remediation

Reviewer `/root` examined immutable initial product `a5d9589`. Its requested
recommendation-loop and identity-fencing fixes are in this cumulative follow-up:
initial automatic catalog retrieval, contextual refresh after successfully
recorded submission, reset of composer/query/cards/mode across profile/session
changes, ignored late old-owner retrieval/recording completions, and immediate
snapshot hiding when configuration is removed or replaced. Unresolved catalog
placeholders block submission. Tests exercise each asynchronous race.

Export follows `3efefd6`: bounded 50-row pages, owner-resolved opaque UUID cursors,
same-session/client checks around every page, repeated-cursor rejection and a
final session refresh before download. The download combines all pages using the
first session/usage snapshot; cancellation discards partial data. Four tests cover
multipage assembly, late owner changes, local cancellation and repeated cursors.

Self-review also fixed manual retry of a live socket after database unavailability
and portrait terminal/control scrolling when the intelligence panel is expanded.

Final staged build PASS with process-only public configuration:
`NEXT_PUBLIC_TERMINUS_WSS_ENDPOINT=wss://sai.tailf8dcea.ts.net/terminal`,
`NEXT_PUBLIC_TERMINUS_WEB_ORIGIN=https://sai.tailf8dcea.ts.net`,
`NEXT_PUBLIC_TERMINUS_CONNECT_MODE=private`; command `npm run build`. This is a
private same-origin staging build for root's Chrome run, not a Vercel deployment.
No environment files or production settings changed. Root owns browser testing.

Final reviewer `/root` passed cumulative `24e1821471785f8416ba155abedb2c729b06ab85`
after inspecting original client/pages and final policy/identity fences,
recommendation flow, placeholder guards and paginated export. It independently
ran `npm test -- components/IntelligencePanel.test.tsx intelligence/export.test.ts
intelligence/useIntelligence.test.tsx`: PASS (three files, 12 tests). The approved
final alignment removes HTML character-count `minLength` from passwords; the RPC
validator enforces the contract's 12..1024 UTF-8 bytes, covered by multibyte tests.
