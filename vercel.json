{
  "buildCommand": "if [ -d apps/web ]; then cd apps/web; fi; npm run build",
  "installCommand": "if [ -d apps/web ]; then cd apps/web; fi; npm ci",
  "devCommand": "if [ -d apps/web ]; then cd apps/web; fi; npm run dev",
  "framework": "nextjs"
}
